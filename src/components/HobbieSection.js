import React, {useState} from 'react';
import styles from '../styles/ToBe.module.css';

const HobbieSection = (props) => {
  const { direction } = useState(props)
  return (
    <div>

    </div>
  )
}

export default HobbieSection